package entity;

import java.awt.*;
import java.util.Random;

public class Apple extends Fruit {

    public Apple(Random rand, int screenWidth, int screenHeight, int unitSize, Color color) {
        super(rand, screenWidth, screenHeight, unitSize, color);
    }
}
